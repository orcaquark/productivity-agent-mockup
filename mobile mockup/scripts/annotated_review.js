// annotated_review.html has no interactive behavior — it's a static
// design-review page (the iframes load the other three mockups, which
// carry their own scripts). This file exists for structural parity
// with the other three pages.
